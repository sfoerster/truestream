# Test Frames

Place test video frames here for inference testing.

Frame files should be 224x224 RGBA raw pixel data files (.raw) or PNG images.
These are not committed to git by default due to size.

To generate test frames:
```bash
npm run generate-session-fixtures
```
